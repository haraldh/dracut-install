name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: install libkmod-dev
      run: sudo apt-get install libkmod-dev

    - name: Build
      run: cargo build --verbose

    - name: Run tests
      run: cargo test --verbose
